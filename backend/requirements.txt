# 数字纪念 · Python 依赖清单
# 用 conda env wechatmsg (Python 3.10) 测试通过
# 安装：pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# Web 框架
fastapi==0.139.0
uvicorn[standard]==0.50.1
python-multipart==0.0.32
aiohttp==3.14.1

# LLM
openai==2.44.0
httpx==0.28.1
httpx-ws==0.9.0

# OCR + RAG + TTS
paddleocr==2.7.3
paddlepaddle==2.6.2
sentence-transformers==5.6.0
transformers==5.13.0
torch==2.12.1
huggingface_hub==1.22.0

# 声音 (TTS + ASR)
edge-tts==7.2.8
faster-whisper==1.2.1
fish-audio-sdk==1.3.0

# 音视频处理
av==17.1.0
numpy
dashscope

# 工具
pefile==2024.8.26  # 用于查看 DLL 依赖（开发/调试用）
